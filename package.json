{
  "name": "socrata-opendata-rdw",
  "version": "1.0.0",
  "repository": "git@github.com:apeixinho/socrata-opendata-rdw.git",
  "license": "GPL-3.0",
  "scripts": {
    "clean": "rimraf dist",
    "build": "yarn run clean && webpack --progress --mode=development",
    "build:prod": "yarn run clean && webpack --progress --mode=production",
    "start": "node ./bin/www",
    "start:dev": "yarn run build &&  webpack-dev-server --hot --progress --mode=development",
    "start:prod": "yarn run build:prod &&  webpack-dev-server --progress --mode=production --host 0.0.0.0"
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "morgan": "~1.9.0",
    "rimraf": "^2.6.3",
    "webpack": "^4.28.3",
    "webpack-cli": "^3.2.0",
    "webpack-dev-server": "^3.1.14"
  }
}
